apparently the line height is:
    line-height = line_spacing * (ascent + descent + line_gap)
        where line_gap is in the .ttf file
        8 bytes after hhea

compiling to wasm:
  /build-wasm$ emcmake cmake .. -DBUILD_WASM=ON
  /build-wasm$ emmake make
